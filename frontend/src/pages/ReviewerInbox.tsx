import React from 'react';
import ReviewThreadWorkspace from '../components/review/ReviewThreadWorkspace';

const ReviewerInbox: React.FC = () => {
  return <ReviewThreadWorkspace mode="reviewer" />;
};

export default ReviewerInbox;
