import React from 'react';
import ReviewThreadWorkspace from '../components/review/ReviewThreadWorkspace';

const AdminReviewInbox: React.FC = () => {
  return <ReviewThreadWorkspace mode="admin" />;
};

export default AdminReviewInbox;
